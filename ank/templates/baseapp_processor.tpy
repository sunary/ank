from ank.components.pipe_app import PipeApp


class {0}(PipeApp):

    def start(self):
        for i in range(10):
            self.chain_process(dict(content=i))

    def process(self, message=None):
        return message
